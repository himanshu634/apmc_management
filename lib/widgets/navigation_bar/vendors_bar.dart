import 'package:flutter/material.dart';

class VendorsBar extends StatelessWidget {
  const VendorsBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("vendors"),
      ),
    );
  }
}
