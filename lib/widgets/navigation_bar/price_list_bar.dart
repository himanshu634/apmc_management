import 'package:flutter/material.dart';

class PriceListBar extends StatelessWidget {
  const PriceListBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("price- list"),
    );
  }
}
